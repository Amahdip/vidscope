// Encoding concepts for the glossary: rate control, quantization, profiles and levels, presets.
// Same shape as CONCEPTS in glossary.js: [term, name, description, more?].

export const ENCODING_CONCEPTS = [
  ['rate-control', 'Rate control', 'How an encoder decides how many bits each frame, and each part of a frame, gets. It is the central trade-off of encoding: constant quality (CRF) lets the file size follow the content, bitrate modes (ABR, CBR, two-pass) fix the size, and VBV caps short peaks for players with limited bandwidth.', 'File insights shows the rate control of x264 and x265 encodes, read from the settings they store in the stream.'],
  ['crf', 'CRF (constant rate factor)', 'A quality target instead of a bitrate target: the encoder varies the bits from scene to scene so that the perceived quality stays about the same. Lower values mean better quality and bigger files, and the size is only known after encoding.', 'x264\'s default is 23 and x265\'s is 28 (the scales differ). FFmpeg\'s H.264 guide calls 17–28 the useful range for x264. CRF is the usual choice for files that are stored or downloaded and, with VBV caps, for streaming.'],
  ['capped-crf', 'Capped CRF', 'CRF combined with VBV limits (a maximum rate and a buffer size). The quality target decides most frames, but the bitrate may not rise above what the buffer model allows: easy content stays small, hard content is held to the cap.', 'Common for streaming, because it saves bits on easy content while keeping every peak deliverable.'],
  ['abr', 'ABR (average bitrate)', 'A bitrate target reached in a single pass: the encoder adjusts the quantizer as it goes so that the whole file averages out at the target. Without seeing the rest of the video it places bits less well than CRF or two passes, and peaks are only limited if VBV is set as well.'],
  ['cbr', 'CBR (constant bitrate)', 'A bitrate that stays nearly the same all the time. In x264 and x265 it is an average target equal to the VBV maximum rate, with a small buffer; filler data can pad it to an exactly constant rate. Broadcast and some live links need it, but hard scenes get no more bits than easy ones.'],
  ['two-pass', 'Two-pass encoding', 'The encoder runs twice: the first pass analyses the whole video and writes statistics to a log file, the second uses them to hit a bitrate target with the bits placed where they help most. It gives predictable sizes with quality close to CRF, at twice the encoding time.'],
  ['vbv', 'VBV and HRD', 'The video buffering verifier (VBV, the encoders\' name) and the hypothetical reference decoder (HRD, the standards\' name) model a player that receives data at a fixed rate into a buffer of fixed size and takes each frame out when it is due. A stream conforms if that buffer never runs dry.', 'Encoders enforce it with a maximum rate and a buffer size, and each codec level sets upper limits for both; the level check in File insights uses the same model.'],
  ['maxrate-bufsize', 'maxrate and bufsize', 'FFmpeg\'s names for the VBV settings: -maxrate is the rate at which the player\'s buffer fills (the bandwidth), -bufsize the size of the buffer. bufsize ÷ maxrate is how many seconds of data the buffer holds, so it sets how long the bitrate may stay above maxrate.'],
  ['qp', 'QP (quantization parameter)', 'How coarsely the transformed picture is rounded. Small QPs keep detail and cost bits; large QPs throw detail away. In H.264 and HEVC it runs from 0 to 51 for 8-bit video (higher bit depths extend the range), and every 6 steps double the rounding step. Rate control works by choosing the QP of each frame and block.'],
  ['lookahead', 'Look-ahead', 'The frames an encoder buffers and analyses before deciding the type and bits of the current one, so that it sees a scene cut, a fade or a static shot coming. More look-ahead gives better decisions but adds latency and memory (rc-lookahead in x264 and x265).'],
  ['mb-tree', 'Macroblock-tree / CU-tree', 'x264\'s mb-tree and x265\'s cu-tree use the look-ahead to measure how much each block is reused as a prediction by later frames, and give more quality to those that are (a static background seen for seconds) and less to those that vanish at once (fast motion, noise). A large efficiency gain for most content.'],
  ['aq', 'Adaptive quantization (AQ)', 'Varies the quantizer inside each frame: flat, smooth areas (sky, walls, gradients) get finer quantization because blocking and banding show there, and busy textures coarser quantization because the eye misses errors in them. It usually lowers PSNR but looks better.'],
  ['psy', 'Psychovisual optimization', 'Encoder tuning for what the eye sees rather than what a metric measures. Psy-rd and psy-rdoq keep the texture and grain (the "energy") of the source even when a smoother, blurrier picture would score better. Tune psnr and tune ssim switch them off for measurements.'],
  ['profile', 'Profile', 'The set of coding tools a stream may use, so that simpler decoders can refuse streams they cannot handle: H.264 Baseline (no B-frames, no CABAC), Main and High (adds 8×8 transforms); HEVC Main (8-bit) and Main 10 (10-bit, used for HDR); AV1 Main, High and Professional. It is written in the parameter sets and the codec string.'],
  ['level', 'Level', 'A cap on how demanding a stream is: picture size, frames or samples per second, bitrate, buffer and the memory for reference frames. A device that supports a level (and profile) can play every stream within it. The level is in the SPS and the codec string: avc1.64001F is High profile, level 3.1.', 'File insights checks each video stream against its level and finds the lowest level it actually fits.'],
  ['tier', 'Tier (HEVC, AV1)', 'HEVC and AV1 levels come in two tiers with the same picture sizes and frame rates but different bitrate limits: Main for consumer delivery, High for professional and very high-quality uses. The High tier starts at level 4.'],
  ['bpp', 'Bits per pixel', 'Bitrate ÷ (width × height × frame rate): the average number of bits spent on each pixel of each frame. It puts encodes of different resolutions and frame rates on one scale, but it is not a quality score: the codec, the resolution and above all the content decide how many bits a good picture needs.'],
  ['preset', 'Preset', 'A named bundle of encoder settings that trades encoding speed for compression: ultrafast, superfast, veryfast, faster, fast, medium (the default), slow, slower, veryslow and placebo in x264 and x265. Slower presets search harder and make smaller files at the same quality.', 'The preset name is not stored in the video; Vidscope compares the options an encoder recorded with each preset.'],
  ['tune', 'Tune', 'Adjusts a preset for a kind of content or use: film, animation, grain, stillimage, psnr, ssim, fastdecode and zerolatency in x264 (x265 has psnr, ssim, grain, zerolatency, fastdecode and animation). Most change psychovisual and filter settings; zerolatency removes B-frames and look-ahead.'],
];
