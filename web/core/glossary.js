// General concepts shown in every file's glossary, whatever the container.

import { ENCODING_CONCEPTS } from './encoding-glossary.js';

export const CONCEPTS = [
  ['offset', 'Offset', 'A byte’s position counted from the start of the file, starting at 0. Vidscope shows offsets in decimal and in hexadecimal (0x…); the hex view has 16 bytes per row, so the row label is the offset of its first byte.'],
  ['big-endian', 'Byte order', 'Multi-byte numbers in MP4, Matroska and MPEG-TS are big-endian: the most significant byte comes first, so the four bytes 00 00 00 20 mean 32. RIFF formats (AVI, WAV) are little-endian: 20 00 00 00 means 32.'],
  ['fourcc', 'Four-character code', 'Four bytes that read as ASCII letters (\'moov\', \'avc1\', \'RIFF\'), used as type names because they are compact and easy to spot in a hex dump.'],
  ['container', 'Container vs codec', 'The container (MP4, MKV, TS, AVI) is the file structure: it holds the streams, their timing and an index. The codec (H.264, HEVC, AV1, AAC, Opus) compresses each stream. The same H.264 frames can be carried in any container, which is why remuxing (-c copy) is lossless and fast.'],
  ['track', 'Track / stream', 'One sequence of media of a single kind (video, audio, subtitles, timecode, metadata) with its own timing and codec.'],
  ['sample', 'Sample / frame / access unit', 'One unit of media with one timestamp: a video frame, or one packet of compressed audio (an AAC frame holds 1024 audio samples per channel). MP4 calls it a sample; Matroska a frame in a block; MPEG-TS carries it in a PES packet.'],
  ['keyframe', 'Key frame / sync sample', 'A frame that can be decoded without any other frame (an IDR frame in H.264, an IRAP picture in HEVC, a key frame in AV1/VP9). Decoding, seeking and stream switching can only start at one.'],
  ['gop', 'GOP (group of pictures)', 'A key frame and the frames that depend on it, up to the next key frame. The GOP length decides how precisely a player can seek and where streaming segments can be cut. Typical values are 1–2 s for streaming and up to 10 s for storage.'],
  ['b-frames', 'B-frames and reordering', 'Frames predicted from both an earlier and a later picture. The later picture has to be decoded first, so the file stores frames in decoding order, which differs from display order; each frame then needs both a decode time and a presentation time.'],
  ['i-frame', 'I-frame (intra frame)', 'A frame coded on its own, like a still image: no other frame is needed to decode it. I-frames are the largest frames, and decoding can only start at one. The Frames view shows them as orange bars.'],
  ['p-frame', 'P-frame (predicted frame)', 'A frame that stores only what changed since earlier frames: motion vectors saying which blocks moved where, plus corrections. Much smaller than an I-frame, but it cannot be decoded without the frames it refers to.'],
  ['idr', 'IDR frame', 'Instantaneous decoding refresh: an H.264/HEVC I-frame after which no frame refers to anything before it. The decoder can start or restart cleanly here, which is why streams are cut and seeked at IDR frames. In H.264 the first slice of an IDR frame has nal_unit_type 5.'],
  ['open-gop', 'Open and closed GOPs', 'In a closed GOP no frame refers to anything before its first frame. In an open GOP, the frames stored right after the I-frame but shown before it (leading frames) may use the previous GOP, so a player that starts at that I-frame has to skip them. Open GOPs compress slightly better; streaming usually requires closed GOPs.'],
  ['cra-rasl', 'CRA, RASL and RADL (HEVC)', 'HEVC names its entry frames IDR (closed GOP), CRA (clean random access, open GOP) and BLA (a CRA after a splice). The frames that follow an entry frame in the file but are shown before it are leading pictures: RASL (random access skipped leading) frames use the previous GOP and are skipped when playback starts at the CRA; RADL (decodable leading) frames are not.'],
  ['recovery-point', 'Recovery point (H.264)', 'An SEI message that marks a non-IDR frame as a place where decoding can start: the picture is correct from there, or after a given number of frames (gradual intra refresh). x264 uses it for open GOPs and for --intra-refresh.'],
  ['reference-frame', 'Reference and non-reference frames', 'A reference frame is kept by the decoder because later frames are predicted from it. A non-reference frame is used once and thrown away, so a player that falls behind can drop it without damaging the picture. H.264 marks this with nal_ref_idc, HEVC with the _N/_R NAL unit types.'],
  ['b-pyramid', 'B-pyramid (hierarchical B-frames)', 'With several B-frames in a row, the encoder can code the middle one first and let the others predict from it. That middle B-frame is then a reference (B) and the rest are not (b). It saves bits at the cost of a little more delay; x264 enables it by default (b-pyramid=normal).'],
  ['decode-order', 'Decoding order vs display order', 'Frames are stored in the order a decoder needs them. A B-frame needs the next reference frame first, so that later frame is stored earlier; the presentation time (PTS) of each frame says when to show it. The Frames view shows both orders side by side.'],
  ['alt-ref', 'Hidden frames and alternate reference (VP9, AV1)', 'VP9 and AV1 have no B-frames. Instead they code a future picture early as a hidden frame (decoded but not shown) and predict the frames in between from it. The picture is shown later, often with a tiny "show existing frame" packet.'],
  ['scene-cut', 'Scene-cut key frames', 'Encoders insert an extra key frame when the picture changes completely, because predicting from the previous scene would cost more bits than starting fresh. It makes the GOP length irregular, so streaming encodes often turn it off (x264: -sc_threshold 0).'],
  ['keyframe-interval', 'Key frame interval and streaming', 'HLS and DASH split a video into segments of a few seconds, and every segment must start with a key frame so players can switch between qualities there. Streaming encodes therefore use a fixed key frame interval that divides the segment length, such as a key frame every 2 s for 2, 4 or 6 s segments (FFmpeg: -g, -keyint_min, -force_key_frames).'],
  ['temporal-layers', 'Temporal layers', 'Frames can be split into layers so that dropping the highest layer halves the frame rate without breaking decoding (temporal scalability). HEVC and AV1 carry the layer number (TemporalId) in every NAL unit or OBU header.'],
  ['pts', 'PTS and DTS', 'Presentation time stamp: when a frame is shown. Decode time stamp: when it must be decoded. They are equal without B-frames. MP4 stores DTS in stts and the difference (PTS − DTS) in ctts; MPEG-TS stores both in PES headers.'],
  ['timescale', 'Timescale', 'How many ticks make one second for a set of timestamps. Timestamps are integers in these ticks, which keeps timing exact: 90,000 is common for video, the sample rate (e.g. 48,000) for audio, 1,000 for milliseconds.'],
  ['bitrate', 'Bitrate', 'Bits per second of media. Average bitrate = size × 8 ÷ duration. With variable bitrate (VBR) complex scenes get more bits; constant bitrate (CBR) keeps the rate steady for broadcast and fixed-bandwidth links, padding if needed.'],
  ['interleaving', 'Interleaving', 'Storing audio and video data close together in time order, so that reading the file sequentially delivers both streams together. Poor interleaving forces players to jump around the file.'],
  ['fast-start', 'Fast start / progressive download', 'An MP4 whose index (moov) comes before the media (mdat). A browser can start playing while the file is still downloading. Encoders usually write moov last; FFmpeg’s -movflags +faststart moves it to the front.'],
  ['fragmented', 'Fragmented MP4 and CMAF', 'An MP4 split into fragments (moof + mdat), each with its own small index. Adaptive streaming (DASH, HLS) serves these fragments as segments; CMAF is the standard profile of fragmented MP4 shared by both.'],
  ['nal-unit', 'NAL unit', 'Network Abstraction Layer unit: the packets an H.264, H.265 or H.266 bitstream is made of. A 1-byte (H.264) or 2-byte (H.265) header says what the unit holds: a coded slice, a parameter set, an SEI message, a delimiter.'],
  ['annex-b', 'Annex B vs AVCC framing', 'Two ways of separating NAL units. Annex B (raw .h264 files, MPEG-TS) puts a start code 00 00 01 before each unit and repeats SPS/PPS in the stream. AVCC/HVCC (MP4, Matroska) prefixes each unit with its length, usually 4 bytes, and keeps SPS/PPS in the codec configuration box.'],
  ['parameter-sets', 'Parameter sets (VPS, SPS, PPS)', 'Stream-wide settings that coded pictures refer to: resolution, profile and level, chroma format, bit depth, reference frame counts and the VUI (colour and timing). A decoder cannot decode anything before it has them.'],
  ['sei', 'SEI message', 'Supplemental enhancement information: side data in the video stream that decoding does not need, such as encoder settings (x264 writes all its options), HDR mastering metadata, closed captions, recovery points or timecode.'],
  ['slice', 'Slice', 'A coded picture or part of one. I-slices are self-contained; P-slices predict from earlier pictures; B-slices from two directions. The slice header says which picture it belongs to and how it is coded.'],
  ['emulation-prevention', 'Emulation prevention byte', 'Inside a NAL unit the byte pattern 00 00 0x must not appear, because it would look like a start code. Encoders insert 03 after every 00 00 (making 00 00 03), and decoders remove it again before reading fields. Vidscope removes them too, and maps each field back to the file bytes.'],
  ['exp-golomb', 'Exp-Golomb code', 'The variable-length integer code used in H.264/H.265 headers: n zero bits, a one, then n more bits. Small numbers take few bits: 0 is "1", 1 is "010", 2 is "011". Fields coded this way are marked ue(v) or se(v).'],
  ['obu', 'OBU (AV1)', 'Open Bitstream Unit: AV1’s equivalent of a NAL unit. Each has a header with a type (sequence header, frame, tile group, metadata...) and usually a size, coded as leb128.'],
  ['profile-level', 'Profile and level', 'The profile says which coding tools a stream may use (e.g. H.264 Baseline, Main, High); the level caps resolution, frame rate and bitrate. Together they tell a device, before decoding, whether it can play the stream.'],
  ['codec-string', 'Codec string (RFC 6381)', 'A short string such as avc1.64001F, hvc1.2.4.L120.B0, av01.0.08M.10 or mp4a.40.2 that encodes the codec, profile and level. Browsers (MediaSource.isTypeSupported), DASH and HLS manifests use it to decide whether a stream is playable.'],
  ['chroma', 'Chroma subsampling', 'Colour detail stored at lower resolution than brightness. 4:2:0 (half horizontally and vertically) is used for almost all delivery video; 4:2:2 and 4:4:4 keep more colour detail for production.'],
  ['bit-depth', 'Bit depth', 'Bits per colour sample: 8-bit gives 256 levels, 10-bit 1024. HDR and high-quality masters use 10-bit or more to avoid banding.'],
  ['colour', 'Colour description', 'Three code points say how to turn pixel values into colours: primaries (BT.709, BT.2020...), transfer function (BT.709 gamma, PQ, HLG) and matrix (how Y′CbCr maps to RGB), plus range: limited (16–235 for 8-bit) or full (0–255). If they are missing, players guess.'],
  ['hdr', 'HDR (PQ and HLG)', 'High dynamic range video uses a transfer function that can express much brighter highlights: PQ (SMPTE ST 2084, used by HDR10 and Dolby Vision) or HLG (broadcast). HDR10 adds static mastering metadata (MaxCLL, MaxFALL, mastering display).'],
  ['par', 'Pixel aspect ratio', 'The shape of each pixel. Square pixels are 1:1. Anamorphic video stores non-square pixels, e.g. 1440×1080 shown as 1920×1080, and must be stretched on display.'],
  ['aac', 'AAC and AudioSpecificConfig', 'AAC audio needs a 2+ byte configuration (object type such as LC or HE-AAC, sample rate, channel layout) before decoding. MP4 keeps it in esds; MPEG-TS instead repeats it in an ADTS header before every frame.'],
  ['priming', 'Encoder delay (priming)', 'Audio encoders such as AAC add a few hundred samples of silence at the start. Containers record how much to skip (MP4 edit lists, Opus pre-skip, Matroska CodecDelay) so audio stays in sync with video.'],
  ['drm', 'Encryption and DRM (CENC)', 'Common Encryption encrypts the media samples (AES-CTR \'cenc\' or AES-CBC with a pattern \'cbcs\') while leaving the container readable. A key ID (KID) names the key, and pssh boxes carry what each DRM system (Widevine, PlayReady, FairPlay) needs to fetch a licence.'],
  ['remux', 'Remux', 'Copying the compressed streams into a new container without re-encoding (ffmpeg -c copy). Quality is unchanged and it is fast; only the container structure changes.'],
];

export function conceptEntries() {
  return [...CONCEPTS, ...ENCODING_CONCEPTS].map(([term, name, desc, more]) => ({ term, name, desc, more, cat: 'concept' }));
}
